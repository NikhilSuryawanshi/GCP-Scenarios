# App Engine

This folder holds App Engine scenarios (standard and flexible environment) with Terraform examples and repro instructions.

Structure
- `scenario-name/files/` — scenario-specific code and documentation belong under `files/`.

Example
- `app-engine/version-routing/files/` — contains `README.md`, `main.tf`, and verification steps.

See the repository root README for repository structure, running instructions, and contribution guidance.
